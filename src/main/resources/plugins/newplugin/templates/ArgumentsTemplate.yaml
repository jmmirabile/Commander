# ${PLUGIN_NAME_CAPITALIZED} Plugin CLI Arguments

# Optional config section for plugin settings
# Uncomment to use a custom help file instead of auto-generated help
# config:
#   help_text_file: "/help.txt"

options:
  - name: help
    short: h
    description: "Display help information"
    required: false
    hasArg: false

  # TODO: Add your custom arguments here
  # Example:
  # - name: input              # Primary identifier - use this in code: cmd.getOptionValue("input")
  #   short: i                 # Short option for command line: -i
  #   description: "Input file path"
  #   required: true
  #   hasArg: true
  #   argHelpLabel: "file"    # Displays as: -i,--input <file>